# Generated by ai-stack dns-watcher (docker-gen) — do not edit manually.
# CoreDNS reloads this file every 15 s.
# Format: <container-IP>  <container-name>
#
# Each running container gets one entry using its first available network IP.
# This lets services resolve container names like "ai-stack.ollama" internally.
# Note: nginxproxy/docker-gen 0.16+ exposes Networks as a slice, not a map.
{{- range $c := . }}
{{- if and $c.State.Running $c.Networks }}
{{- with index $c.Networks 0 }}
{{- if .IP }}
{{ .IP }}	{{ $c.Name }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
